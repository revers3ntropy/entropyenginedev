export class NAME extends JSBehaviour {
    constructor() {
        super();

        // visible in the editor, accessible by this.publicVariable
        this.addPublic({
            name: 'publicVariable',
            value: ''
        });
    }

    Start () {
        // runs once before the first frame

    }

    Update () {
        // runs once per frame, before anything else is run

    }
}